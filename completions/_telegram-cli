#compdef telegram-cli

# AUTOMATICALLY GENERATED by `shtab`


_shtab_telegram_cli_commands() {
  local _commands=(
    
  )
  _describe 'telegram-cli commands' _commands
}

_shtab_telegram_cli_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--init[Login and generate StringSession token.]"
  "--list-chats[List recent chats.]"
  "--search-messages[Search message text across all chats.]:search_messages:"
  "--search-contacts[Search users, groups, channels by name or username.]:search_contacts:"
  "--search-chat[Search messages within a specific chat (requires --chat).]:search_chat:"
  "--chat[Chat username or ID.]:chat:"
  "--from-id[Start message ID.]:from_id:"
  "--to-id[End message ID.]:to_id:"
  "--inclusive[Include boundary messages.]"
  "--forward[Read newer messages.]"
  "--backward[Read older messages.]"
  "--send-message[Text to send.]:send_message:"
  "--send-files[Files to send.]:send_files:"
  "--reply-to[Message ID to reply to.]:reply_to:"
  "--target-chat[Target chat for forward\/cross-chat reply.]:target_chat:"
  "--click-button[Button text to click.]:click_button:"
  "--message-id[Message ID for actions.]:message_id:"
  "--forward-message[Forward message.]"
  "--reply-message[Reply text.]:reply_message:"
  "--download[Download file from message.]"
  "--add-reaction[Add reaction emoji.]:add_reaction:"
  "--edit-message[New text for message.]:edit_message:"
  "--delete-message[Delete a message. Requires --chat and --message-id.]"
  "--get-entities[Get info for users\/chats by ID or username.]:get_entities:"
  "--filter[Media type filter.]:filter:(photo video document url voice gif music round_video mentions pinned)"
  "--min-date[Min date in ISO format (e.g. 2026-01-01).]:min_date:"
  "--max-date[Max date in ISO format.]:max_date:"
  "--groups-only[Search in groups only.]"
  "--users-only[Search in private chats only.]"
  "--broadcasts-only[Search in channels only.]"
  "--limit[Number of items to fetch.]:limit:"
  "--profile[Filtering profile from profiles.json.]:profile:"
  "--bot[Bot mode (token from config.yaml telegram.bot_token).]"
  "--incoming-only[Filter only incoming messages.]"
  "--outgoing-only[Filter only outgoing messages.]"
  "--from-user[Filter messages from specific user ID.]:from_user:"
  "--pattern[Filter messages matching regex pattern.]:pattern:"
  "--has-media[Filter messages with media only.]"
  "--forwarded-only[Filter only forwarded messages.]"
  "--replies-only[Filter only reply messages.]"
  "--has-reactions[Filter messages with reactions only.]"
)

# guard to ensure default positional specs are added only once per session
_shtab_telegram_cli_defaults_added=0


_shtab_telegram_cli() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*' default='*::: :->telegram-cli'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_telegram_cli_defaults_added )); then
    if (( ${_shtab_telegram_cli_options[(I)${(q)one_or_more}*]} +          ${_shtab_telegram_cli_options[(I)${(q)remainder}*]} +          ${_shtab_telegram_cli_options[(I)${(q)default}]} == 0 )); then
      _shtab_telegram_cli_options+=(': :_shtab_telegram_cli_commands' '*::: :->telegram-cli')
    fi
    _shtab_telegram_cli_defaults_added=1
  fi
  _arguments -C -s $_shtab_telegram_cli_options

  case $state in
    telegram-cli)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_telegram_cli-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_telegram_cli -N telegram-cli
else
  # autoload from fpath, call function directly
  _shtab_telegram_cli "$@"
fi

