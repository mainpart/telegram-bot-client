apiVersion: v1
kind: Service
metadata:
  name: telegram-bot-client
  namespace: default
  labels:
    app: telegram-bot-client
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: telegram-bot-client

